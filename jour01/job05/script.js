function afficherJoursSemaines() {
    // Array containing the days of the week
    var joursSemaines = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"];
    
    // Loop through the days of the week and display each one
    for (var i = 0; i < joursSemaines.length; i++) {
        console.log(joursSemaines[i]);
    }
}
